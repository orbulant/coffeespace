DOSSIER — Jordan Reyes (cand_001)

Five sources on ONE candidate, deliberately inconsistent:
  resume.txt          — optimistic, slightly stale ("open to relocation", lists no TS)
  linkedin.txt        — confirms backend/leadership; TypeScript NOT among skills
  call-transcript.txt — admits TS is rusty; says needs remote; comp ~210 flexible
  email-thread.txt    — FRESHEST: remote-first (hybrid won't work) AND comp now 220 firm

The contradictions (this is the point — see /SOLUTION-NOTES.md for the full list):
  • Location: resume "open to relocation / onsite ok"  vs  email "remote-first, hybrid won't work"
  • Comp:     seed.json + call say "210, flexible"      vs  email "220, firm"
  • Skills:   role requires strong TypeScript           vs  call/LinkedIn say TS is rusty/absent

A summarization-only AI feature will flatten these and produce a confident, wrong write-up.
A good AI feature should: pick a source-of-truth rule (e.g. freshest artifact wins),
flag the conflicts, express uncertainty, and surface Open Questions instead of inventing answers.

Note: seed.json still shows Jordan's OLD comp (210/flexible) and Oakland/4-weeks. That mismatch
between the structured record and the latest email is intentional — it mirrors how a real CRM
lags behind the newest conversation.
