# esbuild's native binary resolves from the platform package without its postinstall,
# so the ignored-build-scripts warning is cosmetic. Skip pnpm's pre-run install check
# so `pnpm dev` / `pnpm db:*` don't fail on that warning.
verify-deps-before-run=false
